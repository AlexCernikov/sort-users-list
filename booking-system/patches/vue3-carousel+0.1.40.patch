diff --git a/node_modules/vue3-carousel/dist/carousel.css b/node_modules/vue3-carousel/dist/carousel.css
index 8b76a1c..d4d9e7e 100644
--- a/node_modules/vue3-carousel/dist/carousel.css
+++ b/node_modules/vue3-carousel/dist/carousel.css
@@ -1,6 +1,6 @@
 .carousel {
   position: relative;
-  text-align: center;
+  /* text-align: center; */
   box-sizing: border-box;
 }
 
@@ -17,15 +17,17 @@
 
 .carousel__viewport {
   overflow: hidden;
+  position: relative;
+  /* am adaugat aici position */
 }
 :root {
   /* Color */
-  --vc-clr-primary: #642afb;
+  /* --vc-clr-primary: red; */
   --vc-clr-secondary: #8e98f3;
   --vc-clr-white: #ffffff;
 
   /* Icon */
-  --vc-icn-width: 1.2em;
+  /* --vc-icn-width: 1.2em; */
 
   /* Navigation */
   --vc-nav-width: 30px;
@@ -56,26 +58,27 @@
   fill: currentColor;
 }
 .carousel__pagination {
+  margin-top: 2rem;
   display: flex;
   justify-content: center;
   list-style: none;
 }
 .carousel__pagination-button {
-  margin: var(--vc-pgn-margin);
-  width: var(--vc-pgn-width);
+  margin: 0px;
+  padding: 0px;
+  width: 2rem;
   height: var(--vc-pgn-height);
-  border-radius: var(--vc-pgn-height);
   border: 0;
   cursor: pointer;
-  background-color: var(--vc-pgn-background-color);
+  background-color: #DFDFDF;
 }
 
 .carousel__pagination-button--active {
-  background-color: var(--vc-pgn-active-color);
+  background-color: #DD2C00;
 }
 .carousel__prev,
 .carousel__next {
-  background-color: var(--vc-nav-background-color);
+  /* background-color: var(--vc-nav-background-color);
   border-radius: var(--vc-nav-width);
   width: var(--vc-nav-width);
   height: var(--vc-nav-width);
@@ -87,11 +90,22 @@
   justify-content: center;
   align-items: center;
   position: absolute;
-  border: 0;
+  border: 0; */
   cursor: pointer;
+  /* Am adaugat astea doua de mai jos */
+  position: absolute;
+  top: -15%;
+}
+/* Am adaugat astea de mai jos */
+.carousel__prev{
+  left:70%;
+}
+
+.carousel__next{
+  left:75%;
 }
 
-.carousel__prev {
+/* .carousel__prev {
   top: 50%;
   left: 0;
   transform: translate(-50%, -50%);
@@ -113,4 +127,4 @@
   right: auto;
   left: 0;
   transform: translate(-50%, -50%);
-}
+} */
diff --git a/node_modules/vue3-carousel/dist/carousel.es.js b/node_modules/vue3-carousel/dist/carousel.es.js
index 17a0093..e21fb5f 100644
--- a/node_modules/vue3-carousel/dist/carousel.es.js
+++ b/node_modules/vue3-carousel/dist/carousel.es.js
@@ -591,8 +591,8 @@ const Navigation = (props, { slots, attrs }) => {
     const currentSlide = inject('currentSlide', ref(1));
     const nav = inject('nav', {});
     const isRTL = config.dir === 'rtl';
-    const prevButton = h('button', {
-        type: 'button',
+    const prevButton = h('div', {
+        type: 'image',
         class: [
             'carousel__prev',
             !config.wrapAround &&
@@ -603,8 +603,8 @@ const Navigation = (props, { slots, attrs }) => {
         'aria-label': `Navigate to previous slide`,
         onClick: nav.prev,
     }, (slotPrev === null || slotPrev === void 0 ? void 0 : slotPrev()) || h(Icon, { name: isRTL ? 'arrowRight' : 'arrowLeft' }));
-    const nextButton = h('button', {
-        type: 'button',
+    const nextButton = h('div', {
+        type: 'image',
         class: [
             'carousel__next',
             !config.wrapAround &&
@@ -701,7 +701,7 @@ const Pagination = () => {
         const item = h('li', { class: 'carousel__pagination-item', key: slide }, button);
         children.push(item);
     }
-    return h('ol', { class: 'carousel__pagination' }, children);
+    return h('div', { class: 'carousel__pagination' }, children);
 };
 
 export { Carousel, Icon, Navigation, Pagination, Slide };
